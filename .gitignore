# Python
__pycache__/
*.py[cod]
*$py.class
*.log
.DS_Store
.ipynb_checkpoints

# Environments
.env
.venv
env/
venv/
ENV/

# Runtime run outputs
results/runs/

# Cython build artifacts — do NOT commit generated/compiled files.
# Handwritten C++ sources live in cy_modules/src/lib/ and ARE tracked;
# only the Cython-generated .cpp in cy_modules/ root and the compiled .so are ignored.
*.so
cy_modules/build/
cy_modules/*.cpp
cy_modules/*.c

# Files exceeding GitHub's 100 MB hard limit (upload separately, e.g. release assets / LFS)
Data/large_graphs/Flickr.txt
results/large_graph_runs/Flickr/G.el
